# Actual configuration files (keep only .example files in repo)
sip.conf
extensions.conf
voicemail.conf
docker-compose.yml
msmtprc

# Development documentation (not for public repo)
CLAUDE.md

# README template (don't ignore - needed for auto-update)
# README.md.dist

# Vim swap files
*.swp
*.swo
